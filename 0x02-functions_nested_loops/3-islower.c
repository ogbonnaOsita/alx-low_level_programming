#include "main.h"
/**
 * _islower - check for lowercase character
 * letter - the char to be checked
 * Return: Always 1 (Success)
 */

int _islower(int letter)
{
	return (letter >= 'a'  && letter <= 'z');
}
