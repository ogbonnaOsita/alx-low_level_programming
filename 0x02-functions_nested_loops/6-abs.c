#include "main.h"
/**
 * _abs - computes the absolute value for an integer
 * @n: the number to check
 * Return: the required value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
